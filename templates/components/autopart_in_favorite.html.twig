<div class="card">
    <div class="row g-0 align-items-center">
        <a class="col-sm-4 text-decoration-none" href="{{ path('autopart', {autopartId: autopart.autopartId}) }}">
            <img
                class="img-fluid rounded-start ms-3"
                src="{{ asset('img/components/gearshift.svg') }}"
                alt="{{ autopart.title }}"
            >
        </a>
        <div class="col-sm-8">
            <div class="card-body">
                <a class="d-block text-decoration-none text-dark" href="{{ path('autopart', {autopartId: autopart.autopartId}) }}">
                    <h5 class="card-title">
                        {{ autopart.title }}
                    </h5>
                    <p class="card-text">
                        Для {{ autopart.car }}
                    </p>
                </a>
                <div class="row g-3 justify-content-between align-items-center mt-3">
                    <hr class="mb-2">
                    <div
                        class="col-6 btn autopart-card-btn autopart-card-like-btn liked align-items-center m-0"
                        data-autopart-id="{{ autopart.autopartId }}"

                        {% if is_granted('ROLE_USER') %}
                            data-user-id="{{ app.user.userId }}"
                        {% endif %}
                    ></div>
                    <div
                        class="col-6 btn autopart-card-btn autopart-card-cart-btn align-items-center m-0"
                        data-autopart-id="{{ autopart.autopartId }}"

                        {% if is_granted('ROLE_USER') %}
                            data-user-id="{{ app.user.userId }}"
                        {% endif %}
                    ></div>
                </div>
            </div>
        </div>
    </div>
</div>