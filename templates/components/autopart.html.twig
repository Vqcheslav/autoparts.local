<div class="card mb-3">
    <div class="row g-0">
        <div class="col-md-4">
            <img
                class="img-fluid rounded-start ms-3"
                src="{{ asset('img/components/gearshift.svg') }}"
                alt="{{ autopart.title }}"
            >
        </div>
        <div class="col-md-8">
            <div class="card-body">
                <h5 class="card-title">
                    {{ autopart.title }}
                </h5>
                <p class="card-text">
                    Для {{ autopart.car }}
                </p>
                <p class="card-text">
                    Склад: {{ autopart.warehouse }}
                </p>
                <p class="card-text">
                    {{ autopart.description|raw }}
                </p>
            </div>
        </div>
    </div>
</div>